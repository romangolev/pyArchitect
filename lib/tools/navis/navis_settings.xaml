<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="pyArchitect Navisworks settings" Width="560" Height="390"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Navisworks 3D view name" />
        <TextBox x:Name="tbViewName" Grid.Row="1" Margin="0,4,0,12" />
        <TextBlock Grid.Row="2" Text="Default profile" />
        <ComboBox x:Name="cbProfile" Grid.Row="3" Margin="0,4,0,12" />
        <StackPanel Grid.Row="4">
            <CheckBox x:Name="cbHideLinks" Margin="0,0,0,8"
                      Content="Hide Revit links in the Navisworks view" />
            <CheckBox x:Name="cbRecreate" Margin="0,0,0,12"
                      Content="Recreate an existing view during batch processing" />
            <Button x:Name="btnEditProfiles" Width="160" HorizontalAlignment="Left"
                    Content="Edit profile presets..." />
        </StackPanel>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnCancel" Width="90" Margin="0,0,6,0" Content="Cancel" />
            <Button x:Name="btnSave" Width="90" Content="Save" />
        </StackPanel>
    </Grid>
</Window>
