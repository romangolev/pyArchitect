<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource ProfileEditorTitle}" Width="620" Height="680"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="{DynamicResource ProfileLabel}" />
        <StackPanel Grid.Row="1" Margin="0,4,0,12">
            <DockPanel>
                <Button x:Name="btnAddProfile" Width="100" DockPanel.Dock="Right" Margin="6,0,0,0"
                        Content="{DynamicResource AddPreset}" />
                <Button x:Name="btnDeleteProfile" Width="100" DockPanel.Dock="Right" Margin="6,0,0,0"
                        Content="{DynamicResource DeletePreset}" />
                <ComboBox x:Name="cbProfile" />
            </DockPanel>
            <Grid Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="145" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock VerticalAlignment="Center" Text="{DynamicResource PresetNameLabel}" />
                <TextBox x:Name="tbProfileName" Grid.Column="1" />
                <TextBlock Grid.Row="1" VerticalAlignment="Center" Margin="0,6,0,0"
                           Text="{DynamicResource NameMarkersLabel}" />
                <TextBox x:Name="tbNameMarkers" Grid.Row="1" Grid.Column="1" Margin="0,6,0,0"
                         ToolTip="{DynamicResource NameMarkersTooltip}" />
            </Grid>
        </StackPanel>
        <TextBlock Grid.Row="2"
                   Text="{DynamicResource CategoriesHint}"
                   TextWrapping="Wrap" />
        <ListBox x:Name="lbCategories" Grid.Row="3" Margin="0,8,0,8" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnCancel" Width="90" Margin="0,0,6,0" Content="{DynamicResource Cancel}" />
            <Button x:Name="btnSave" Width="90" Content="{DynamicResource Save}" />
        </StackPanel>
    </Grid>
</Window>
